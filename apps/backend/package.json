{
  "name": "backend",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "uv run fastapi dev main.py",
    "export-openapi": "uv run python export_openapi.py",
    "build": "uv run python export_openapi.py && echo 'Backend validated'",
    "lint": "uv run ruff check .",
    "lint:fix": "uv run ruff check --fix .",
    "format": "uv run ruff format .",
    "format:check": "uv run ruff format --check ."
  }
}
